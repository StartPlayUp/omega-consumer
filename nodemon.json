{ 
    "verbose": true,
    "watch": ["server/**/*.ts"], 
    "ext": "ts js json",
    "ignore": ["server/**/*.spec.ts"],
    "exec": "ts-node --files --project tsconfig.server.json server/index.ts"
}
